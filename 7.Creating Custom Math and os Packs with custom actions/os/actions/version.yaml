---
name: version
description: This action is used to get the os version
runner_type: "local-shell-cmd"
enabled: true
entry_point: ""
parameters:
  cmd:
    type: string
    default: cat /etc/os-release | grep -w "VERSION=" |cut -d "=" -f 2
    immutable: true
    required: true

