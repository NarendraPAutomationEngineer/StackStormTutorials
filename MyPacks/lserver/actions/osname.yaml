---
name: OSName
runner_type: "local-shell-cmd"
enabled: true
entry_point: ""
pack: lserver
parameters:
    cmd:
      type: "string"
      default: 'cat /etc/os-release | grep -w "NAME" | cut -d "=" -f 2'

